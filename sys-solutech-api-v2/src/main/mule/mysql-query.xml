<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
	xmlns:db="http://www.mulesoft.org/schema/mule/db"
	xmlns="http://www.mulesoft.org/schema/mule/core"
	xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
http://www.mulesoft.org/schema/mule/db http://www.mulesoft.org/schema/mule/db/current/mule-db.xsd
http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">

	<flow name="mysql-query-main" doc:id="mysql-query-main-0001">
		<logger level="INFO" doc:name="Start MySQL query"
			message='#[output text/plain --- "[Sys-Solutech] GET /" ++ ((vars.endpoint default "") as String) ++ " started. resource=" ++ ((attributes.queryParams.resource default "") as String) ++ " database=" ++ ((attributes.queryParams.database default "") as String) ++ " fromDate=" ++ ((attributes.queryParams.fromDate default "") as String) ++ " toDate=" ++ ((attributes.queryParams.toDate default "") as String) ++ " limit=" ++ ((attributes.queryParams.limit default "") as String) ++ " offset=" ++ ((attributes.queryParams.offset default "") as String)]' />
		<ee:transform doc:name="Paging vars">
			<ee:message />
			<ee:variables>
				<ee:set-variable variableName="databaseName"><![CDATA[attributes.queryParams.database default p("db.name")]]></ee:set-variable>
				<ee:set-variable variableName="offset"><![CDATA[(attributes.queryParams.offset default 0) as Number]]></ee:set-variable>
				<ee:set-variable variableName="limit"><![CDATA[(attributes.queryParams.limit default p("db.limit") default 10000) as Number]]></ee:set-variable>
			</ee:variables>
		</ee:transform>
		<choice doc:name="Build SQL for endpoint">
			<when expression="#[lower(vars.endpoint default '') == 'product']">
				<ee:transform doc:name="Product SQL">
					<ee:message />
					<ee:variables>
						<ee:set-variable variableName="sql"><![CDATA[%dw 2.0
output text/plain
---
"SELECT * FROM " ++ vars.databaseName ++ "." ++ p("db.table.products") ++ " WHERE supplier = :supplier ORDER BY id LIMIT " ++ (((vars.limit as Number) + 1) as String) ++ " OFFSET " ++ (vars.offset as String)]]></ee:set-variable>
						<ee:set-variable variableName="sqlParams"><![CDATA[%dw 2.0
output application/java
---
{
	supplier: attributes.queryParams.supplier default p("db.invoice.supplier") default ""
}]]></ee:set-variable>
					</ee:variables>
				</ee:transform>
				<flow-ref doc:name="mysql-query-subflow" name="mysql-query-subflow" />
				<flow-ref doc:name="mysql-paged-response-subflow" name="mysql-paged-response-subflow" />
				<logger level="INFO" doc:name="End product query"
					message='#[output text/plain --- "[Sys-Solutech] GET /product completed. records=" ++ (sizeOf(payload.data default []) as String) ++ " hasMore=" ++ ((vars.hasMore default false) as String)]' />
			</when>
			<when expression="#[(lower(vars.endpoint default '') == 'sale') or (lower(vars.endpoint default '') == 'invoice')]">
				<ee:transform doc:name="Invoice SQL">
					<ee:message />
					<ee:variables>
						<ee:set-variable variableName="sql"><![CDATA[%dw 2.0
output text/plain
var db = vars.databaseName
---
"Select a.*, b.routeid
from " ++ db ++ "." ++ p("db.table.sales") ++ " as a
left join " ++ db ++ "." ++ p("db.table.visits") ++ " as b on a.VisitID = b.visitid
inner join " ++ db ++ "." ++ p("db.table.products") ++ " as c on a.PRODUCT_ID = c.id and c.supplier = :supplier
where a.Created_AT between :fromTs and :toTs
order by a.Entry_ID desc
LIMIT " ++ (((vars.limit as Number) + 1) as String) ++ " OFFSET " ++ (vars.offset as String)]]></ee:set-variable>
						<ee:set-variable variableName="sqlParams"><![CDATA[%dw 2.0
output application/java
---
{
	supplier: p("db.invoice.supplier") default "",
	fromTs: (attributes.queryParams.fromDate default "") ++ " 00:00:00",
	toTs: (attributes.queryParams.toDate default "") ++ " 00:00:00"
}]]></ee:set-variable>
					</ee:variables>
				</ee:transform>
				<flow-ref doc:name="mysql-query-subflow" name="mysql-query-subflow" />
				<flow-ref doc:name="mysql-paged-response-subflow" name="mysql-paged-response-subflow" />
				<logger level="INFO" doc:name="End invoice query"
					message='#[output text/plain --- "[Sys-Solutech] GET /" ++ ((vars.endpoint default "invoice") as String) ++ " completed. records=" ++ (sizeOf(payload.data default []) as String) ++ " hasMore=" ++ ((vars.hasMore default false) as String)]' />
			</when>
			<otherwise>
				<logger level="INFO" doc:name="Unknown endpoint"
					message='#[output text/plain --- "[Sys-Solutech] GET /" ++ ((vars.endpoint default "") as String) ++ " has no SQL mapping. Set resource to Invoice or Product."]' />
			</otherwise>
		</choice>
	</flow>

	<sub-flow name="mysql-query-subflow" doc:id="mysql-query-subflow-0001">
		<logger level="INFO" doc:name="Run SQL"
			message='#[output text/plain --- "[Sys-Solutech] executing query for /" ++ ((vars.endpoint default "") as String) ++ " database=" ++ ((vars.databaseName default "") as String) ++ " limit=" ++ ((vars.limit default "") as String) ++ " offset=" ++ ((vars.offset default "") as String)]' />
		<db:select doc:name="MySQL select" config-ref="MySQL_Config">
			<db:sql><![CDATA[#[vars.sql]]]></db:sql>
			<db:input-parameters><![CDATA[#[vars.sqlParams default {}]]]></db:input-parameters>
		</db:select>
	</sub-flow>

	<sub-flow name="mysql-paged-response-subflow" doc:id="mysql-paged-response-0001">
		<ee:transform doc:name="Page data and next URL">
			<ee:message>
				<ee:set-payload><![CDATA[%dw 2.0
output application/json
var rows = payload default []
var limitNum = vars.limit as Number default 10000
var offsetNum = vars.offset as Number default 0
var hasMore = (sizeOf(rows) default 0) > limitNum
var data = if (hasMore and limitNum > 0) rows[0 to (limitNum - 1)] else rows
var host = (attributes.headers.host default attributes.headers.Host default "localhost:8081") as String
var path = (attributes.requestPath default "/api/datashare") as String
var scheme = (attributes.scheme default "http") as String
var resource = (attributes.queryParams.resource default vars.endpoint default "") as String
var database = (attributes.queryParams.database default vars.databaseName default "") as String
var fromDate = (attributes.queryParams.fromDate default "") as String
var toDate = (attributes.queryParams.toDate default "") as String
var supplier = (attributes.queryParams.supplier default "") as String
var nextOffset = offsetNum + limitNum
var next = scheme ++ "://" ++ host ++ path
	++ "?resource=" ++ resource
	++ "&database=" ++ database
	++ "&fromDate=" ++ fromDate
	++ "&toDate=" ++ toDate
	++ "&limit=" ++ (limitNum as String)
	++ "&offset=" ++ (nextOffset as String)
	++ (if (supplier != "") ("&supplier=" ++ supplier) else "")
---
{
	data: data,
	next: if (hasMore) next else null
}]]></ee:set-payload>
			</ee:message>
			<ee:variables>
				<ee:set-variable variableName="hasMore"><![CDATA[%dw 2.0
output application/java
var rows = payload default []
var limitNum = vars.limit as Number default 10000
---
(sizeOf(rows) default 0) > limitNum]]></ee:set-variable>
			</ee:variables>
		</ee:transform>
	</sub-flow>
</mule>
