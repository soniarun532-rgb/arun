{
  "info": {
    "_postman_id": "sat-sys-solutech-sapi-localhost",
    "name": "SAT SAPI localhost",
    "description": "APIkit GETs on local SAPI (8081). No resource query param. fromDate/toDate are YYYY-MM-DD only. Follow payload.next until it is null.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    { "key": "baseUrl", "value": "http://localhost:8081" },
    { "key": "database", "value": "sat_nobleoutlook" },
    { "key": "fromDate", "value": "2026-08-13" },
    { "key": "toDate", "value": "2026-08-14" },
    { "key": "limit", "value": "10000" },
    { "key": "offset", "value": "0" },
    { "key": "supplier", "value": "BIC EAST AFRCA LIMITED" }
  ],
  "item": [
    {
      "name": "GET /invoice",
      "request": {
        "method": "GET",
        "header": [],
        "url": "{{baseUrl}}/api/invoice?database={{database}}&fromDate={{fromDate}}&toDate={{toDate}}&limit={{limit}}&offset={{offset}}"
      }
    },
    {
      "name": "GET /order",
      "request": {
        "method": "GET",
        "header": [],
        "url": "{{baseUrl}}/api/order?database={{database}}&fromDate={{fromDate}}&toDate={{toDate}}&limit={{limit}}&offset={{offset}}"
      }
    },
    {
      "name": "GET /product",
      "request": {
        "method": "GET",
        "header": [],
        "url": "{{baseUrl}}/api/product?database={{database}}&supplier={{supplier}}&limit={{limit}}&offset={{offset}}"
      }
    },
    {
      "name": "GET /route",
      "request": {
        "method": "GET",
        "header": [],
        "url": "{{baseUrl}}/api/route?database={{database}}&limit={{limit}}&offset={{offset}}"
      }
    },
    {
      "name": "GET /sales-rep",
      "request": {
        "method": "GET",
        "header": [],
        "url": "{{baseUrl}}/api/sales-rep?database={{database}}&limit={{limit}}&offset={{offset}}"
      }
    },
    {
      "name": "GET /store",
      "request": {
        "method": "GET",
        "header": [],
        "url": "{{baseUrl}}/api/store?database={{database}}&limit={{limit}}&offset={{offset}}"
      }
    },
    {
      "name": "GET /inventory",
      "request": {
        "method": "GET",
        "header": [],
        "url": "{{baseUrl}}/api/inventory?database={{database}}&limit={{limit}}&offset={{offset}}"
      }
    },
    {
      "name": "GET /warehouse",
      "request": {
        "method": "GET",
        "header": [],
        "url": "{{baseUrl}}/api/warehouse?database={{database}}&limit={{limit}}&offset={{offset}}"
      }
    }
  ]
}
