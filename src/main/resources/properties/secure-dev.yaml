# Values wrapped in ![ ] are decrypted with -M-Dsecure.key=<key>
# Encrypt with:
# java -jar secure-properties-tool.jar string encrypt AES CBC <secure.key> "<value>"
process:
  api:
    clientId: "REPLACE_DEV_CLIENT_ID"
    clientSecret: "REPLACE_DEV_CLIENT_SECRET"
